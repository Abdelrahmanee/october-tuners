version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: october-tuners
    ports:
      - '3000:3000'
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
