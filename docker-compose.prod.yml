version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: october-tuners-prod
    ports:
      - '3000:3000'
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: always
